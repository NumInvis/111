# 2026-05-29 Work Log

## Game Design Redesign Proposal
- Diagnosed API 500 root cause: LLM JSON fails WorldBlueprintSchema strict validation + AuditService re-throw + no global exception filter
- Identified fundamental design philosophy error: math is core mechanic instead of world-building flavor
- Authored REDESIGN_PROPOSAL_v1.md with 3-phase plan

## v3 Architecture Redesign — FULLY IMPLEMENTED
- Created world-book.ts (packages/game-engine/src/constants/) — 14 realm→mathLevel hardcoded mapping
- Deleted AttributeNameEnum (8 fixed attrs) → AttributeDefSchema (3-12 dynamic AI-generated attrs)
- AttributeSchema: fixed 8-field object → z.record(z.string(), z.number())
- Deleted REALM_ADVANCEMENT_THRESHOLDS → RealmAdvancementRuleSchema (AI-generated per world)
- Deleted ANNUAL_ATTRIBUTE_GROWTH/BREAKTHROUGH_COST/LIFESPAN_EXTENSION → read from WorldBlueprint
- RealmAdvancementChecker: now takes advancementRules[] constructor param
- Field renames: mathematicalStrength→specialty, mathematicalDoctrine→philosophy, mathematicalContribution→legacy
- WorldBlueprint: added attributeDefs, advancementRules, startingState; removed stateModel
- GenerationPreferences DELETED → zero-param world generation
- Schema min constraints relaxed (clues 2-15, rumors 1-10, events 2-8)
- All 5 prompts rewritten: world_generation v3 (zero-param + world book), npc_dialogue v3 (realm-math constraints), event_generation v3 (dynamic attr names)
- API: GenerationService.generateWorld() takes no preferences, uses WorldBlueprint.startingState
- API: GenerationController accepts empty body; GenerationPreferencesDto DELETED
- API: game.service.ts RealmAdvancementChecker instantiated per call with blueprint rules
- Python: models.py synced (added AttributeDef/RealmAdvancementRule/StartingState, deleted GenerationPreferences)
- Python: world_generator.py and routes/world.py updated for zero-param generation
- Frontend: world-gen.tsx simplified to single "开天辟地" button; api.ts generateWorld() takes only sessionId
- Frontend: types/index.ts — deleted ATTRIBUTE_KEYS/ATTRIBUTE_LABELS, Attribute=Record<string,number>, added REALM_MATH_LEVELS
- Frontend: StatusPanel/NPCProfile/EventCard/death/journal — updated from hardcoded to dynamic attributes
- Frontend: gameStore — removed preference/setPreference
- **Build: ALL packages and apps pass typecheck + build**
